[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ks-contracts"
version = "0.1.0"
description = "Contratos e schemas compartilhados do KS Atendimento IA"
authors = [{ name = "KaelSolutions" }]
dependencies = [
    "pydantic>=2.7.0",
]
requires-python = ">=3.11"
